# Belt-and-suspenders: this repo must NEVER contain Claude secrets or transcripts.
# (The repo lives outside ~/.claude, so these shouldn't appear here anyway.)
*.credentials*
.credentials.json
.claude.json
*.jsonl
# ...except the synthetic, public-safe session fixtures the test suite renders (no real transcripts).
!tests/fixtures/*.jsonl
projects/

# agent worktrees (`EnterWorktree`, or `git worktree add .claude/worktrees/<name>`). A worktree is a
# whole nested checkout: unignored it reads as untracked content, and any `git add -A` in the parent
# sweeps it up. Ignore it whether or not you use one.
.claude/worktrees/

# editor / OS noise
*.swp
.DS_Store

# python (the mcp/ server runs via `uv run --script`; no build artifacts belong here)
__pycache__/
*.pyc

# mkdocs build output (CI builds + gh-deploys; the local `site/` is a throwaway artifact)
site/

# kcov output. Not just noise: the HTML report bundles third-party JavaScript into the tree, and
# test_license_headers.sh lists sources with `git ls-files --cached --others`, so an unignored
# report is read as ~10 new .js "source files" with no SPDX header and reddens the suite. That is
# how the coverage job managed to fail a test the `test` job passed.
coverage/
coverage.xml
